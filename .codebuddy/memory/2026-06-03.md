# 2026-06-03

- Updated CODEBUDDY.md to reflect current codebase state. Key changes: added missing routes (/user/<screen_name>, /downloader, /api/tweet, /delete, /rate), documented 4 separate GraphQL queryId caches (HomeTimeline/UserTweets/Bookmarks/TweetDetail), added _parse_instructions() shared parser, updated key functions table to remove stale line numbers and include new functions, reorganized data files section.

- Homepage redesign: changed index page from fixed 20-video limit + by-author section to infinite-scroll grid showing all downloaded videos (paginated 30/page via /videos/more API). Moved "by author" browsing to new /authors page with /authors route and authors.html template. Updated all template nav bars to include "作者" link. Updated author.html back button from "返回首页" to "返回作者". Added tests for /authors and /videos/more routes.

- Completed COS storage migration research. Generated doc at Development/COS存储迁移调研.md. Key findings: (1) recommend StorageBackend abstraction with Local/COS implementations; (2) video access via presigned URLs (private bucket + temporary auth); (3) thumbnails still use OpenCV but upload result jpg to COS then delete local temp; (4) env vars: COS_REGION/COS_SECRET_ID/COS_SECRET_KEY/COS_BUCKET/STORAGE_BACKEND; (5) estimated cost ~¥37/month for 100GB+50GB traffic.

- Completed COS storage migration implementation. (1) Created `storage.py` with StorageBackend ABC + LocalStorage + CosStorage; (2) Refactored `app.py` to use storage backend for all file operations (upload/download/delete/list/exists/get_url); (3) Updated all templates to use `thumb_url` (presigned URL or local path); (4) Added `cos-python-sdk-v5` to requirements.txt; (5) Added COS env vars to .env.example; (6) Removed all tests (tests/ directory deleted); (7) Updated deploy.sh to remove pytest step; (8) Fixed storage.py cross-platform compatibility (shutil.disk_usage instead of os.statvfs); (9) Updated CODEBUDDY.md to reflect new architecture.
